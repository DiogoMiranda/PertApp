<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="diogo.miranda.pert">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version"

            />


        <activity
            android:name=".View.Activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.Activity.DisplayAtividade"
            android:label="@string/title_activity_display_contact"
            android:parentActivityName=".View.Activity.MainActivity">

        </activity>
        <activity
            android:name=".View.Activity.EstimativaActivity"
            android:label="@string/calculoPert"
            android:parentActivityName=".View.Activity.MainActivity" />




        <activity
            android:name=".View.Activity.SobreActivity"
            android:configChanges="orientation"
            android:label="@string/Sobre"
            android:parentActivityName=".View.Activity.MainActivity"
            android:screenOrientation="portrait">

        </activity>




    </application>

</manifest>